## 1. Retrospective Review

- [x] 1.1 Confirm the shipped account administration workflow in `frontend`, `functions`, and `worker-auth` matches the new OpenSpec delta requirements.
- [x] 1.2 Verify the documented deep-link format `?view=admin&uid=<uid>` and UID-first onboarding email content against the deployed behavior.

## 2. Specification Integration

- [x] 2.1 Merge the new `account-administration` capability into the baseline specs when this change is archived.
- [x] 2.2 Merge the `rw-auth` and `worker-auth` delta specs into the baseline specs when this change is archived.
