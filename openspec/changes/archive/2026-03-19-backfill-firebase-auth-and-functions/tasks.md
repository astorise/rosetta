## 1. Spec Backfill Scope

- [x] 1.1 Identify the OpenSpec baseline gaps between the deployed Firebase onboarding system and the current `openspec/specs/` library.
- [x] 1.2 Define the capability split for deployment, authenticated UI fallback, auth UI behavior, and backend onboarding behavior.

## 2. Artifact Authoring

- [x] 2.1 Write the retrospective proposal describing the missing baseline coverage and the deployed architecture.
- [x] 2.2 Write delta specs for `firebase-deployment`, `teacher-ui`, `rw-auth`, and `worker-auth`.
- [x] 2.3 Write the design document explaining the backfill strategy and the rationale for the Cloud Function to Cloud Run onboarding relay.

## 3. Baseline Synchronization

- [x] 3.1 Validate the change artifacts with the OpenSpec CLI.
- [x] 3.2 Archive the completed change so the new and updated capabilities are synced into `openspec/specs/`.
