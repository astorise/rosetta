## 1. Retroactive Change Capture

- [x] 1.1 Create a dedicated OpenSpec change for the Firebase deployment bootstrap contract
- [x] 1.2 Document why the archived CI/CD changes were insufficient to guarantee a successful first production deploy

## 2. Capability Specification

- [x] 2.1 Add `firebase-deployment` requirements for workflow trigger coverage and manual dispatch
- [x] 2.2 Add `firebase-deployment` requirements for quota-aware ADC auth and deployer IAM expectations
- [x] 2.3 Add `firebase-deployment` requirements for Firebase project bootstrap and production Web App configuration injection

## 3. Review and Consolidation

- [x] 3.1 Validate the retrospective change against the currently deployed workflow and Firebase project state
- [x] 3.2 Archive the change after review so `firebase-deployment` becomes part of the baseline OpenSpec set
